// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
      apiKey: "AIzaSyAhuOmzDiY6JvAISAt4aFfyI09wINf3dmo",
      authDomain: "vende-mais-z.firebaseapp.com",
      projectId: "vende-mais-z",
      storageBucket: "vende-mais-z.appspot.com",
      messagingSenderId: "484503335394",
      appId: "1:484503335394:web:7729a87fc098c0a16d09bb",
      measurementId: "G-TB0C3T31RH"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
